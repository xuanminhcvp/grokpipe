# Môi trường Python (tạo lại trên máy mới)
**/.venv/
**/__pycache__/
*.pyc

# Phiên đăng nhập trình duyệt
**/.chatgpt_profile/
grok_test/

# File tạm / trùng lặp (giữ assets/ là bản chuẩn)
**/tries/
**/candidates/
**/inbox/

# Rác hệ thống
.DS_Store

# File chat thô
*.jsonl

# Ảnh/video sinh ra (nặng — chỉ giữ code, không đẩy media lên git)
PHIM-HEIRESS/assets/
PHIM-HEIRESS/versions/
PHIM-HEIRESS/videos/
*.project/assets/
*.project/videos/
*.project/versions/
*.project/quarantine/
*.project/.thumbs/
*.project/.snapshots/
*.project/assets_v1_16x9/
*.project/assets_v2_fullbody_23/
*.project/run.log
*.project/state.json
Nhân vật/

# video ghép để duyệt (dựng lại được từ videos/ bằng ffmpeg concat)
*.project/*.mp4
# file trạng thái lúc chạy
.sfboard-running.json
.luu-ban.log
.video-gate
*.project/.video-gate

# chat log + snapshot nặng (không bao giờ đẩy lên git)
*.project/videos/versions/

# ⛔ SKILL LÀM PHIM — BÍ QUYẾT RIÊNG, CHỈ NẰM TRÊN MÁY, KHÔNG BAO GIỜ ĐẨY LÊN GIT
.claude/skills/

# bản sao lưu sf-board.json (chỉ có ý nghĩa trên máy đang làm)
*.json.bak-*

# ⛔ DỮ LIỆU PHIM — prompt SF/video/nhạc là bí quyết riêng, chỉ nằm trên máy
*.project/

# ⛔ Công cụ kiểm — mã hoá sẵn ngưỡng của skill, giữ trên máy
sfboard/kiem-luat.py
sfboard/kiem-noi-shot.py

# kho git của repo PRIVATE — không bao giờ đẩy lên repo public
.git-rieng/
day-rieng.sh
